name: Staple macOS Notarization

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release tag (e.g. v0.14.1-rc1)"
        required: true
      mode:
        description: "Mode: 'staple' (use existing submission) or 'resubmit' (fresh notarization)"
        required: true
        default: "resubmit"
        type: choice
        options:
          - resubmit
          - staple
      submission_id:
        description: "Submission ID (only for 'staple' mode)"
        required: false

permissions:
  contents: write

jobs:
  notarize-and-staple:
    runs-on: macos-15
    environment: build
    timeout-minutes: 45
    steps:
      - name: Download DMG from release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ inputs.version }}
        run: |
          gh release download "$VERSION" \
            --pattern "AgentSH-${VERSION}.dmg" \
            --repo "${{ github.repository }}" \
            --dir .
          echo "Downloaded AgentSH-${VERSION}.dmg"

      - name: Extract app from DMG
        env:
          VERSION: ${{ inputs.version }}
        run: |
          hdiutil attach "AgentSH-${VERSION}.dmg" -mountpoint /tmp/agentsh-dmg -nobrowse
          cp -R /tmp/agentsh-dmg/AgentSH.app ./AgentSH.app
          hdiutil detach /tmp/agentsh-dmg

      - name: Submit fresh notarization
        if: inputs.mode == 'resubmit'
        env:
          APPLE_ID: ${{ secrets.MACOS_NOTARIZATION_APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.MACOS_NOTARIZATION_PASSWORD }}
          TEAM_ID: ${{ secrets.MACOS_NOTARIZATION_TEAM_ID }}
        run: |
          ditto -c -k --keepParent AgentSH.app AgentSH.zip

          xcrun notarytool submit AgentSH.zip \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_PASSWORD" \
            --team-id "$TEAM_ID" \
            --wait --timeout 35m

      - name: Check existing submission
        if: inputs.mode == 'staple'
        env:
          APPLE_ID: ${{ secrets.MACOS_NOTARIZATION_APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.MACOS_NOTARIZATION_PASSWORD }}
          TEAM_ID: ${{ secrets.MACOS_NOTARIZATION_TEAM_ID }}
          SUBMISSION_ID: ${{ inputs.submission_id }}
        run: |
          if [ -z "$SUBMISSION_ID" ]; then
            echo "::error::submission_id is required in staple mode"
            exit 1
          fi
          INFO=$(xcrun notarytool info "$SUBMISSION_ID" \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_PASSWORD" \
            --team-id "$TEAM_ID" 2>&1)
          echo "$INFO"
          if echo "$INFO" | grep -q "status: In Progress"; then
            echo "::error::Notarization still in progress. Try again later."
            exit 1
          fi
          if echo "$INFO" | grep -q "status: Invalid"; then
            echo "::error::Notarization was rejected. Check the log:"
            xcrun notarytool log "$SUBMISSION_ID" \
              --apple-id "$APPLE_ID" \
              --password "$APPLE_PASSWORD" \
              --team-id "$TEAM_ID" || true
            exit 1
          fi

      - name: Staple and repackage DMG
        env:
          VERSION: ${{ inputs.version }}
        run: |
          xcrun stapler staple AgentSH.app
          xcrun stapler validate AgentSH.app
          spctl --assess --type exec --verbose=2 AgentSH.app || true

          rm "AgentSH-${VERSION}.dmg"
          hdiutil create -volname "AgentSH" \
            -srcfolder AgentSH.app \
            -ov -format UDZO \
            "AgentSH-${VERSION}.dmg"

      - name: Upload stapled DMG to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ inputs.version }}
        run: |
          gh release upload "$VERSION" "AgentSH-${VERSION}.dmg" \
            --repo "${{ github.repository }}" \
            --clobber
          echo "Uploaded stapled DMG to release $VERSION"
